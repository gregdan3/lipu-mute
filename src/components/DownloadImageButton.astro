---
import IconButton from "@components/IconButton.astro";
---

<span data-copy-image-button>
  <IconButton icon="mingcute:download-2-line" />
</span>

<script>
  import { handleGraphImageRequest } from "@utils/ui";
  const el = document.querySelector("[data-copy-image-button]");
  if (el) {
    el.addEventListener("click", async () => {
      await handleGraphImageRequest();
      document.dispatchEvent(
        new CustomEvent("show-toast", {
          detail: { message: "Downloaded graph image" },
        }),
      );
    });
  }
</script>
